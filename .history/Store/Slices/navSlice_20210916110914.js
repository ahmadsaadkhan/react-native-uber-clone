import { createSlice } from '@reduxjs/toolkit'
const inialState = {
    origin: null,
    destination: null,
    travelTimeInformation: null
}

export const navSlice = createSlice({
    name: 'nav',
    reducers: {
         
    }
})